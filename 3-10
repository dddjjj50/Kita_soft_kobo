#include <stdio.h>

int main(void) {

	while (1) {
		int x = 0;
		printf("数値を入力して");
		scanf("%d", &x);

		int y = 0;
		printf("数値を入力して");
		scanf("%d", &y);

		printf("%d\n", x - y);
		printf("%d\n", x / y);
	}
	return 0;
}
